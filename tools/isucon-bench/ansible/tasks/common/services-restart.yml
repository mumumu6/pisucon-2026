---
# サービス再起動の単一入口。service: nginx | db | app
- name: Nginxを再起動する
  ansible.builtin.systemd:
    name: nginx
    daemon_reload: true
    state: restarted
  become: true
  when: service == 'nginx'

- name: DBを再起動する
  ansible.builtin.systemd:
    name: "{{ mysql_service }}"
    state: restarted
  become: true
  when: service == 'db'

- name: アプリを再起動する
  ansible.builtin.systemd:
    name: "{{ app_service }}"
    state: restarted
  become: true
  when: service == 'app'
