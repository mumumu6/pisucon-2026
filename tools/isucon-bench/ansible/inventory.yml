all:
  vars:
    ansible_user: isucon
  children:
    app:
      hosts:
        isucon-1:
          ansible_host: 18.183.138.21
    db:
      hosts:
        isucon-1:
    nginx:
      hosts:
        isucon-1:
    # 開始案内、スコア入力、統合レポートの基準にするホストを1台指定します。
    reporter:
      hosts:
        isucon-1:
